<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.elad.canvasdrawlite">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name=".myProvider"
            android:authorities="com.example.elad.canvasdrawlite"
            android:exported="false" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ColorPickerActivity" />
        <activity android:name=".SavingDrawActivity" />
        <activity android:name=".DrawActivity" />
        <activity android:name=".MyPaintsActivity" />
        <activity android:name=".DownloadedListActivity" />
        <activity android:name=".RecycleListActivity" />
        <activity android:name=".OnlinePaintsList" />
        <activity android:name=".ShowCodeActivity"></activity>
    </application>

</manifest>